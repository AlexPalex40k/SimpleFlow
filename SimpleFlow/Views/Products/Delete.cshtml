@model Product
@{ ViewData["Title"] = "Delete product"; }
<h1>Delete product</h1><p>Delete <strong>@Model.Sku — @Model.Name</strong>?</p><form asp-action="Delete" asp-route-id="@Model.Id"><button class="btn btn-danger">Delete</button> <a asp-action="Index" class="btn btn-secondary">Cancel</a></form>
